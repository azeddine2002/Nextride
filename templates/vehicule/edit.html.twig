{% extends 'base.html.twig' %}

{% block title %}Modifier un véhicule{% endblock %}

{% block body %}
<div class="page-contenu">
    <h1>Modifier {{ vehicule.marque }} {{ vehicule.modele }}</h1>

    <div class="carte-formulaire">
        {{ include('vehicule/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
    </div>

    <p><a href="{{ path('app_vehicule_index') }}">&larr; retour à la liste</a></p>

    {{ include('vehicule/_delete_form.html.twig') }}
</div>
{% endblock %}
